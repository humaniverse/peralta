peralta <- function() {
  # 1. Investigate the suspects. Map across each suspect, and append information
  #    into evidence. If no existinging evidence is found on disk, create a file.
  # 2. Create a diff: compare latest evidence with existing evidence.
  # 3. Print diff to console (e.g., updates or lack thereof)
}